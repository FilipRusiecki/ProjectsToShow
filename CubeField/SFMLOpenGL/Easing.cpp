#include <Easing.h>

using namespace gpp;

// t = time, b = startvalue, c = change in value, d = duration:
float Easing::easeIn(float t, float b, float c, float d ) 
{
	return 0.001f; // Experiment with t, b, c and d
}
